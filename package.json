{
  "name": "survivor-support",
  "version": "1.0.0",
  "description": "Survivor Support Platform - Backend API",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend-main && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["support", "survivor", "help", "api"],
  "author": "Survivor Support Team",
  "license": "MIT"
}
